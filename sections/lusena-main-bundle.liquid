{%- liquid
  assign current_variant = product.selected_or_first_available_variant
-%}

{%- liquid
  assign override_style = ''
  assign override_style = override_style | append: '--lusena-section-padding-top-desktop:' | append: section.settings.padding_top | append: 'px;'
  assign override_style = override_style | append: '--lusena-section-padding-top-mobile:' | append: section.settings.padding_top_mobile | append: 'px;'
  if section.settings.padding_bottom > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-desktop:' | append: section.settings.padding_bottom | append: 'px;'
  endif
  if section.settings.padding_bottom_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-mobile:' | append: section.settings.padding_bottom_mobile | append: 'px;'
  endif
-%}

{{ 'lusena-pdp.css' | asset_url | stylesheet_tag }}
{{ 'lusena-bundle-pdp.css' | asset_url | stylesheet_tag }}

<section
  class="lusena-pdp lusena-pdp--bundle lusena-spacing--compact"
  data-lusena-product-section
  {% if override_style != blank %}
    style="{{ override_style }}"
  {% endif %}
>
  <script type="application/json" data-lusena-product-json>
    {{ product | json }}
  </script>

  <div class="lusena-container">
    {% render 'lusena-breadcrumbs', page_type: 'product', product: product, collection: collection %}

    <div class="lusena-grid lusena-grid--pdp">
      {% render 'lusena-pdp-media', product: product, current_variant: current_variant, section: section %}

      <div class="lusena-pdp-buy-box">
        <div class="lusena-pdp-buy-box__summary">
          {% render 'lusena-bundle-summary', product: product, current_variant: current_variant, section: section %}
        </div>

        <div class="lusena-pdp-buy-box__proof">
          {% render 'lusena-pdp-proof-chips' %}
        </div>

        <div class="lusena-pdp-buy-box__contents">
          {% render 'lusena-bundle-contents', current_variant: current_variant, section: section %}
        </div>

        <div class="lusena-pdp-buy-box__desktop-divider"></div>

        <div class="lusena-pdp-buy-box__variant">
          {% render 'lusena-bundle-options', current_variant: current_variant, section: section %}
        </div>

        {%- comment -%} Cross-sell checkbox (hidden until all colors picked, revealed by JS) {%- endcomment -%}
        {%- assign cs_handle = section.settings.cross_sell_handle -%}
        {%- assign cs_product = all_products[cs_handle] -%}
        {%- if section.settings.cross_sell_enabled
              and cs_handle != blank
              and cs_product != blank
              and cs_product.available
              and product.handle != cs_handle -%}
          {%- unless product.handle contains 'scrunchie' -%}
            <div class="lusena-pdp-buy-box__cross-sell-checkbox lusena-bundle-cross-sell--hidden"
                 data-lusena-bundle-cross-sell-wrapper>
              {%- render 'lusena-pdp-cross-sell-checkbox',
                cross_sell_product: cs_product,
                cross_sell_price: section.settings.cross_sell_price,
                current_variant: current_variant,
                product: product,
                section: section,
                skip_js: true
              -%}
            </div>
          {%- endunless -%}
        {%- endif -%}

        <div class="lusena-pdp-buy-box__atc">
          {% render 'lusena-bundle-atc', product: product, current_variant: current_variant, section: section %}
        </div>

        <div class="lusena-pdp-buy-box__guarantee">
          {% render 'lusena-pdp-guarantee', section: section %}
        </div>

        <div class="lusena-pdp-buy-box__payment">
          {% render 'lusena-pdp-payment', section: section %}
        </div>

        <div class="lusena-pdp-buy-box__care">
          {% render 'lusena-bundle-care', product: product %}
        </div>

        {%- liquid
          assign benefit_1_text = blank
          assign benefit_2_text = blank
          assign benefit_3_text = blank

          if product != blank
            assign benefit_1_metafield = product.metafields.lusena.pdp_benefit_1
            if benefit_1_metafield != blank
              if benefit_1_metafield.value != blank
                assign benefit_1_text = benefit_1_metafield.value
              endif
            endif

            assign benefit_2_metafield = product.metafields.lusena.pdp_benefit_2
            if benefit_2_metafield != blank
              if benefit_2_metafield.value != blank
                assign benefit_2_text = benefit_2_metafield.value
              endif
            endif

            assign benefit_3_metafield = product.metafields.lusena.pdp_benefit_3
            if benefit_3_metafield != blank
              if benefit_3_metafield.value != blank
                assign benefit_3_text = benefit_3_metafield.value
              endif
            endif
          endif

          assign fallback_benefit_1 = blank
          assign fallback_benefit_2 = blank
          assign fallback_benefit_3 = blank

          for block in section.blocks
            if block.type == 'benefit'
              if fallback_benefit_1 == blank
                assign fallback_benefit_1 = block.settings.text
              else
                if fallback_benefit_2 == blank
                  assign fallback_benefit_2 = block.settings.text
                else
                  if fallback_benefit_3 == blank
                    assign fallback_benefit_3 = block.settings.text
                  endif
                endif
              endif
            endif
          endfor

          if benefit_1_text == blank
            assign benefit_1_text = fallback_benefit_1
          endif
          if benefit_2_text == blank
            assign benefit_2_text = fallback_benefit_2
          endif
          if benefit_3_text == blank
            assign benefit_3_text = fallback_benefit_3
          endif

          assign has_benefit_text = false
          if benefit_1_text != blank
            assign has_benefit_text = true
          else
            if benefit_2_text != blank
              assign has_benefit_text = true
            else
              if benefit_3_text != blank
                assign has_benefit_text = true
              endif
            endif
          endif
        -%}

        {%- if has_benefit_text -%}
          <div class="lusena-pdp-buy-box__benefits-mobile">
            <ul class="lusena-pdp-buy-box__benefits-list"{% if settings.animations_reveal_on_scroll %} data-cascade{% endif %}>
              {%- if benefit_1_text != blank -%}
                <li class="lusena-pdp-buy-box__benefit-item{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
                  <span class="lusena-pdp-buy-box__benefit-dot"></span>
                  {{ benefit_1_text | escape }}
                </li>
              {%- endif -%}
              {%- if benefit_2_text != blank -%}
                <li class="lusena-pdp-buy-box__benefit-item{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
                  <span class="lusena-pdp-buy-box__benefit-dot"></span>
                  {{ benefit_2_text | escape }}
                </li>
              {%- endif -%}
              {%- if benefit_3_text != blank -%}
                <li class="lusena-pdp-buy-box__benefit-item{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
                  <span class="lusena-pdp-buy-box__benefit-dot"></span>
                  {{ benefit_3_text | escape }}
                </li>
              {%- endif -%}
            </ul>
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% render 'lusena-bundle-sticky-atc', product: product, current_variant: current_variant, section: section %}

{% render 'lusena-bundle-scripts' %}

{% schema %}
{
  "name": "LUSENA bundle (PDP)",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Spacing overrides (0 = use global default)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 240,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "Bundle summary"
    },
    {
      "type": "text",
      "id": "emotional_headline",
      "label": "Emotional headline",
      "default": "Kompletna ochrona na noc"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Tagline",
      "default": "Jedwab morwowy Grade 6A, 22 momme, certyfikat OEKO-TEX\u00ae Standard 100. Ka\u017cdy produkt w zestawie to ta sama jako\u015b\u0107 premium."
    },
    {
      "type": "text",
      "id": "savings_label",
      "label": "Savings label",
      "default": "Oszcz\u0119dzasz"
    },
    {
      "type": "text",
      "id": "contents_heading",
      "label": "Contents heading",
      "default": "W zestawie"
    },
    {
      "type": "header",
      "content": "Color selector"
    },
    {
      "type": "text",
      "id": "select_colors_heading",
      "label": "Step counter heading",
      "default": "Wybierz kolory"
    },
    {
      "type": "text",
      "id": "select_color_label",
      "label": "Color selector label",
      "default": "Kolor"
    },
    {
      "type": "text",
      "id": "select_instruction",
      "label": "Color instruction subtitle",
      "default": "Wybierz kolor"
    },
    {
      "type": "header",
      "content": "Add to cart"
    },
    {
      "type": "text",
      "id": "add_to_cart_label",
      "label": "Add to cart label",
      "default": "Dodaj do koszyka"
    },
    {
      "type": "text",
      "id": "add_to_cart_loading_label",
      "label": "Add to cart loading label",
      "default": "Dodaj\u0119..."
    },
    {
      "type": "text",
      "id": "in_stock_label",
      "label": "In-stock label",
      "default": "Na stanie, gotowe do wysy\u0142ki"
    },
    {
      "type": "text",
      "id": "sticky_add_to_cart_label",
      "label": "Sticky ATC label",
      "default": "Dodaj do koszyka"
    },
    {
      "type": "text",
      "id": "buy_now_label",
      "label": "Buy now label",
      "default": "Kup teraz"
    },
    {
      "type": "text",
      "id": "buy_now_loading_label",
      "label": "Buy now loading label",
      "default": "Przekierowuj\u0119..."
    },
    {
      "type": "header",
      "content": "Delivery"
    },
    {
      "type": "text",
      "id": "delivery_eta_label",
      "label": "Delivery ETA",
      "default": "Dostawa: 1\u20132 dni robocze"
    },
    {
      "type": "text",
      "id": "delivery_returns_link_label",
      "label": "Returns link label",
      "default": "60 dni na zwrot"
    },
    {
      "type": "header",
      "content": "Guarantee"
    },
    {
      "type": "text",
      "id": "guarantee_heading",
      "label": "Guarantee heading",
      "default": "60 dni gwarancji spokojnego snu"
    },
    {
      "type": "text",
      "id": "guarantee_link_label",
      "label": "Guarantee link label",
      "default": "Jak to dzia\u0142a?"
    },
    {
      "type": "header",
      "content": "Payment"
    },
    {
      "type": "text",
      "id": "secure_payment_label",
      "label": "Secure payment label",
      "default": "Bezpieczna p\u0142atno\u015b\u0107"
    },
    {
      "type": "header",
      "content": "Cross-sell checkbox"
    },
    {
      "type": "checkbox",
      "id": "cross_sell_enabled",
      "label": "Pokazuj checkbox cross-sell w buy-boxie",
      "default": true
    },
    {
      "type": "text",
      "id": "cross_sell_handle",
      "label": "Handle produktu cross-sell",
      "default": "silk-scrunchie",
      "info": "URL handle produktu (np. silk-scrunchie). Znajdziesz go w Shopify admin > Products > SEO."
    },
    {
      "type": "number",
      "id": "cross_sell_price",
      "label": "Cena cross-sell (grosze)",
      "default": 3900,
      "info": "Cena wyswietlana na PDP. Musi odpowiadac rabatowi BXGY w Shopify admin (np. 3900 = 39 zl)."
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Bundle benefit text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LUSENA bundle (PDP)",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "text": "Oszcz\u0119dzasz w por\u00f3wnaniu do zakupu osobno"
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Produkty zaprojektowane tak, by si\u0119 uzupe\u0142nia\u0107"
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Darmowa dostawa - zestaw kwalifikuje si\u0119 automatycznie"
          }
        }
      ]
    }
  ]
}
{% endschema %}
