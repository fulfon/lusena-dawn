{%- liquid
  assign override_style = ''
  if section.settings.padding_top > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-desktop:' | append: section.settings.padding_top | append: 'px;'
  endif
  if section.settings.padding_bottom > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-desktop:' | append: section.settings.padding_bottom | append: 'px;'
  endif
  if section.settings.padding_top_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-mobile:' | append: section.settings.padding_top_mobile | append: 'px;'
  endif
  if section.settings.padding_bottom_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-mobile:' | append: section.settings.padding_bottom_mobile | append: 'px;'
  endif
-%}
<section
  class="bg-white lusena-returns-steps lusena-spacing--hero"
  {% if override_style != blank %}
    style="{{ override_style }}"
  {% endif %}
>
  <div class="container lusena-returns-steps__container mx-auto">
    <div class="text-center lusena-gap-section-intro">
      <span class="text-accent-gold text-sm tracking-widest uppercase font-semibold lusena-gap-kicker{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
        {{ section.settings.steps_kicker | escape }}
      </span>
      <h2 class="text-3xl md:text-5xl font-serif text-primary{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
        {{ section.settings.steps_heading | escape }}
      </h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 relative" {% if settings.animations_reveal_on_scroll %}data-cascade{% endif %}
>
      <div class="lusena-returns-steps__connector" aria-hidden="true"><span></span></div>

      {%- for block in section.blocks -%}
        {%- if block.type == 'step' -%}
          {%- liquid
            assign icon_name = block.settings.icon | strip | downcase
            case icon_name
              when 'đź›’'
                assign icon_name = 'shopping-bag'
              when 'shopping-bag', 'shopping bag', 'shoppingbag'
                assign icon_name = 'shopping-bag'
              when 'đź“§'
                assign icon_name = 'mail'
              when 'mail', 'email', 'envelope'
                assign icon_name = 'mail'
              when 'đź’°'
                assign icon_name = 'banknote'
              when 'banknote', 'money'
                assign icon_name = 'banknote'
              else
                assign icon_name = 'shopping-bag'
            endcase
          -%}
          <div class="lusena-returns-steps__card-wrap{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}" {{ block.shopify_attributes }}>
            <div class="relative z-10 bg-surface-2 lusena-returns-steps__card p-8 text-center space-y-4">
              <span class="text-accent-gold text-xs tracking-widest uppercase font-semibold">
                {{ section.settings.step_label_prefix | escape }} {{ forloop.index }}
              </span>
              <div class="w-16 h-16 mx-auto bg-white rounded-full flex items-center justify-center text-accent-cta shadow-sm">
                {% render 'lusena-icon', name: icon_name, class: 'lusena-returns-steps__step-icon', stroke_width: 1.5 %}
              </div>
              <h3 class="font-serif text-xl text-primary">{{ block.settings.title | escape }}</h3>
              <p class="text-secondary text-sm leading-relaxed">{{ block.settings.text | escape }}</p>
            </div>
          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>

{% stylesheet %}

  .lusena-returns-steps__container {
    max-width: 102.4rem !important;
  }

  .lusena-returns-steps__card-wrap {
    position: relative;
    z-index: 10;
    height: 100%;
  }

  .lusena-returns-steps__card {
    display: flex;
    flex-direction: column;
    height: 100%;
    border-radius: 0.8rem;
    transition: transform 0.3s;
    will-change: transform;
  }

  @media (hover: hover) and (pointer: fine) {
    .lusena-returns-steps__card:hover {
      transform: translateY(-0.4rem);
    }
  }

  .lusena-returns-steps__connector {
    display: none;
    position: absolute;
    top: 4rem;
    left: 20%;
    right: 20%;
    pointer-events: none;
    z-index: 0;
  }

  .lusena-returns-steps__connector span {
    display: block;
    height: 0;
    border-top: 1px dashed rgb(74 74 74 / 0.2);
  }

  .lusena-returns-steps__step-icon {
    width: 2.8rem;
    height: 2.8rem;
  }

  @media (min-width: 768px) {

    .lusena-returns-steps__connector {
      display: block;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "LUSENA Returns: Steps",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Spacing overrides (0 = use global default)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "text",
      "id": "steps_kicker",
      "label": "Section kicker",
      "default": "Prosty Proces"
    },
    {
      "type": "text",
      "id": "steps_heading",
      "label": "Section heading",
      "default": "Jak to dziaĹ‚a?"
    },
    {
      "type": "text",
      "id": "step_label_prefix",
      "label": "Step label prefix",
      "default": "Krok"
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon name",
          "default": "shopping-bag"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Kupujesz i testujesz"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Masz peĹ‚ne 60 dni od momentu otrzymania paczki. Nie musisz trzymaÄ‡ folii ani metek - po prostu Ĺ›pij i oceniaj."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LUSENA Returns: Steps",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "icon": "shopping-bag",
            "title": "Kupujesz i testujesz",
            "text": "Masz peĹ‚ne 60 dni od momentu otrzymania paczki. Nie musisz trzymaÄ‡ folii ani metek - po prostu Ĺ›pij i oceniaj."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon": "mail",
            "title": "Piszesz do nas",
            "text": "Wystarczy krĂłtki mail na czesc@lusena.pl. Odezwiemy siÄ™ w ciÄ…gu 24h i wyĹ›lemy instrukcjÄ™ zwrotu."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon": "banknote",
            "title": "Szybki zwrot Ĺ›rodkĂłw",
            "text": "PieniÄ…dze wrĂłcÄ… na Twoje konto w ciÄ…gu 3-5 dni roboczych od odebrania przez nas przesyĹ‚ki."
          }
        }
      ]
    }
  ]
}
{% endschema %}
