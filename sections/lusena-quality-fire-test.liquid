{%- liquid
  assign override_style = ''
  if section.settings.padding_top > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-desktop:' | append: section.settings.padding_top | append: 'px;'
  endif
  if section.settings.padding_bottom > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-desktop:' | append: section.settings.padding_bottom | append: 'px;'
  endif
  if section.settings.padding_top_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-mobile:' | append: section.settings.padding_top_mobile | append: 'px;'
  endif
  if section.settings.padding_bottom_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-mobile:' | append: section.settings.padding_bottom_mobile | append: 'px;'
  endif
-%}
<section
  class="bg-brand-bg lusena-quality-fire-test lusena-spacing--standard"
  {% if override_style != blank %}
    style="{{ override_style }}"
  {% endif %}
>
  <div class="container text-center">
    <h2 class="text-3xl md:text-4xl font-serif text-primary lusena-gap-section-intro{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {{ section.settings.fire_heading | escape }}
    </h2>

    <div class="max-w-4xl mx-auto bg-black/5 aspect-video flex items-center justify-center rounded-sm border border-secondary/10 overflow-hidden{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {%- if section.settings.fire_video != blank -%}
        {{
          section.settings.fire_video
          | video_tag:
            controls: true,
            preload: 'metadata',
            class: 'w-full h-full object-cover'
        }}
      {%- else -%}
        <span class="text-gray-500 font-light text-center px-6">
          {{ section.settings.fire_video_placeholder | escape }}
        </span>
      {%- endif -%}
    </div>

    <p class="lusena-gap-body text-secondary font-light max-w-2xl mx-auto{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
      {{ section.settings.fire_body | escape }}
    </p>
  </div>
</section>

{% stylesheet %}
{% endstylesheet %}

{% schema %}
{
  "name": "LUSENA Quality: Fire test",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Spacing overrides (0 = use global default)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "text",
      "id": "fire_heading",
      "label": "Fire heading",
      "default": "Test Ognia (The Fire Test)"
    },
    {
      "type": "video",
      "id": "fire_video",
      "label": "Fire video"
    },
    {
      "type": "text",
      "id": "fire_video_placeholder",
      "label": "Fire video placeholder",
      "default": "Video Placeholder: Test spalania nitki jedwabiu"
    },
    {
      "type": "text",
      "id": "fire_body",
      "label": "Fire body",
      "default": "Prawdziwy jedwab po spaleniu pachnie jak pieczone wĹ‚osy i zamienia siÄ™ w sypki popiĂłĹ‚. Poliester topi siÄ™ w twardÄ… kulkÄ™. Nie mamy nic do ukrycia."
    }
  ],
  "presets": [
    {
      "name": "LUSENA Quality: Fire test"
    }
  ]
}
{% endschema %}
