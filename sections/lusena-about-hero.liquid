{%- liquid
  assign override_style = ''
  if section.settings.padding_top > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-desktop:' | append: section.settings.padding_top | append: 'px;'
  endif
  if section.settings.padding_bottom > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-desktop:' | append: section.settings.padding_bottom | append: 'px;'
  endif
  if section.settings.padding_top_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-mobile:' | append: section.settings.padding_top_mobile | append: 'px;'
  endif
  if section.settings.padding_bottom_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-mobile:' | append: section.settings.padding_bottom_mobile | append: 'px;'
  endif
-%}
<section
  class="bg-white relative overflow-hidden lusena-about-hero lusena-spacing--hero"
  {% if override_style != blank %}
    style="{{ override_style }}"
  {% endif %}
>
  <div class="container max-w-5xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-16 items-center">
    <div{% if settings.animations_reveal_on_scroll %} class="scroll-trigger animate--slide-in"{% endif %} {% if settings.animations_reveal_on_scroll %}data-cascade{% endif %}
>
      <span class="text-accent-gold text-sm tracking-widest uppercase font-semibold lusena-gap-kicker">
        {{ section.settings.hero_kicker | escape }}
      </span>
      <h1 class="text-5xl md:text-7xl font-serif text-primary leading-tight lusena-gap-heading">
        {{ section.settings.hero_heading_line_1 | escape }}<br>
        <span class="italic text-secondary/60">{{ section.settings.hero_heading_line_2 | escape }}</span>
      </h1>
      <div class="text-lg md:text-xl font-light text-secondary space-y-6 leading-relaxed">
        {{ section.settings.hero_body }}
      </div>
    </div>

    <div class="relative h-[600px] w-full bg-neutral-100 rounded-sm overflow-hidden{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}" {% if settings.animations_reveal_on_scroll %}data-cascade{% endif %}
>
      {%- if section.settings.hero_image != blank -%}
        {{
          section.settings.hero_image
          | image_url: width: 1600
          | image_tag:
            class: 'absolute inset-0 w-full h-full object-cover',
            loading: 'lazy',
            widths: '600, 900, 1200, 1600',
            sizes: '(min-width: 768px) 45vw, 90vw'
        }}
      {%- else -%}
        <span class="absolute inset-0 flex items-center justify-center text-gray-400 font-light italic">
          {{ section.settings.hero_image_placeholder | escape }}
        </span>
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
{% endstylesheet %}

{% schema %}
{
  "name": "LUSENA About: Hero",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Spacing overrides (0 = use global default)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "text",
      "id": "hero_kicker",
      "label": "Kicker",
      "default": "O Nas"
    },
    {
      "type": "text",
      "id": "hero_heading_line_1",
      "label": "Heading line 1",
      "default": "Born in Suzhou."
    },
    {
      "type": "text",
      "id": "hero_heading_line_2",
      "label": "Heading line 2 (italic)",
      "default": "Perfected in Poland."
    },
    {
      "type": "richtext",
      "id": "hero_body",
      "label": "Hero body",
      "default": "<p>Wierzymy, ĹĽe sen to najwaĹĽniejszy zabieg kosmetyczny dnia.</p><p>Przez lata traktowaliĹ›my baweĹ‚nianÄ… poĹ›ciel jako \"standard\". Ale baweĹ‚na jest chĹ‚onna jak rÄ™cznik â€” wypija wilgoÄ‡ z Twojej skĂłry i kremu na noc.</p><p>StworzyliĹ›my LUSENÄ, aby zamieniÄ‡ Twoje 8 godzin snu w pasywnÄ… pielÄ™gnacjÄ™. Bez wysiĹ‚ku. Po prostu kĹ‚adziesz gĹ‚owÄ™.</p>"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero image"
    },
    {
      "type": "text",
      "id": "hero_image_placeholder",
      "label": "Hero image placeholder",
      "default": "ZdjÄ™cie portretowe (Founder / Mood board)"
    }
  ],
  "presets": [
    {
      "name": "LUSENA About: Hero"
    }
  ]
}
{% endschema %}
