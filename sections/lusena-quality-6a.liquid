{%- liquid
  assign override_style = ''
  if section.settings.padding_top > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-desktop:' | append: section.settings.padding_top | append: 'px;'
  endif
  if section.settings.padding_bottom > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-desktop:' | append: section.settings.padding_bottom | append: 'px;'
  endif
  if section.settings.padding_top_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-top-mobile:' | append: section.settings.padding_top_mobile | append: 'px;'
  endif
  if section.settings.padding_bottom_mobile > 0
    assign override_style = override_style | append: '--lusena-section-padding-bottom-mobile:' | append: section.settings.padding_bottom_mobile | append: 'px;'
  endif
-%}
<section
  class="bg-white lusena-quality-6a lusena-spacing--standard"
  {% if override_style != blank %}
    style="{{ override_style }}"
  {% endif %}
>
  <div class="container grid grid-cols-1 md:grid-cols-2 gap-16 items-center">
    <div class="bg-neutral-100 aspect-square flex items-center justify-center rounded-sm overflow-hidden{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}" {% if settings.animations_reveal_on_scroll %}data-cascade{% endif %}
>
      {%- if section.settings.image != blank -%}
        {{
          section.settings.image
          | image_url: width: 1400
          | image_tag:
            class: 'w-full h-full object-cover',
            loading: 'lazy',
            widths: '600, 900, 1200, 1400',
            sizes: '(min-width: 768px) 45vw, 90vw'
        }}
      {%- else -%}
        <span class="text-gray-400 font-light italic text-center px-6">
          {{ section.settings.image_placeholder | escape }}
        </span>
      {%- endif -%}
    </div>

    <div class="lusena-content-flow{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}" {% if settings.animations_reveal_on_scroll %}data-cascade{% endif %}
>
      <div class="lusena-content-flow--tight">
        {%- if section.settings.kicker != blank -%}
          <span class="text-accent-gold text-sm tracking-widest uppercase font-semibold">
            {{ section.settings.kicker | escape }}
          </span>
        {%- endif -%}
        <h2 class="text-3xl md:text-4xl font-serif text-primary">
          {{ section.settings.heading | escape }}
        </h2>
      </div>

      <div class="lusena-content-flow--tight text-secondary text-lg font-light leading-relaxed">
        {{ section.settings.body }}
      </div>

      {%- if section.blocks.size > 0 -%}
        <ul class="lusena-content-flow--tight">
          {%- for block in section.blocks -%}
            {%- if block.type == 'benefit' -%}
              <li class="flex flex-nowrap items-start gap-3" {{ block.shopify_attributes }}>
                <span
                  class="w-1.5 h-1.5 rounded-full bg-accent-gold shrink-0"
                  style="margin-top: calc((1.625em - 0.375rem) / 2);"
                  aria-hidden="true"
                ></span>
                <span class="min-w-0 flex-1">{{ block.settings.text | escape }}</span>
              </li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
{% endstylesheet %}

{% schema %}
{
  "name": "LUSENA Quality: 6A",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Spacing overrides (0 = use global default)"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom (desktop)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Padding top (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Padding bottom (mobile)",
      "min": 0,
      "max": 300,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker",
      "default": "Klasa jedwabiu"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Klasa 6A"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body",
      "default": "<p>Klasa 6A to najwyższa klasyfikacja jedwabiu morwowego. Oznacza dłuższe, bardziej jednorodne włókna oraz stabilną jakość partii.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Infographic / image"
    },
    {
      "type": "text",
      "id": "image_placeholder",
      "label": "Image placeholder",
      "default": "Infografika: skala klas jedwabiu (3A → 6A)"
    }
  ],
  "blocks": [
    {
      "type": "benefit",
      "name": "Benefit",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Wyższa selekcja włókien i powtarzalna jakość."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "LUSENA Quality: 6A",
      "blocks": [
        {
          "type": "benefit",
          "settings": {
            "text": "Dłuższe, bardziej jednorodne włókna."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Stabilna jakość partii i wykończenia."
          }
        },
        {
          "type": "benefit",
          "settings": {
            "text": "Delikatny połysk i gładkość w dotyku."
          }
        }
      ]
    }
  ]
}
{% endschema %}

